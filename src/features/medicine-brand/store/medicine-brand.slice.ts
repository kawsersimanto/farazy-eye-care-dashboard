import { createSlice } from "@reduxjs/toolkit";

export const medicineBrandSlice = createSlice({
  name: "medicine-brand",
  initialState: {},
  reducers: {},
});

export const {} = medicineBrandSlice.actions;
export const medicineBrandReducer = medicineBrandSlice.reducer;
