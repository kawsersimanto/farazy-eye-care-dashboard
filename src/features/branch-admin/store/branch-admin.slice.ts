import { createSlice } from "@reduxjs/toolkit";

export const branchAdminSlice = createSlice({
  name: "branch-admin",
  initialState: {},
  reducers: {},
});

export const {} = branchAdminSlice.actions;
export const branchAdminReducer = branchAdminSlice.reducer;
