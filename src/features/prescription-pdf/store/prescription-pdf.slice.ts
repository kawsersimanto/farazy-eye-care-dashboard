import { createSlice } from "@reduxjs/toolkit";

export const prescriptionPdfSlice = createSlice({
  name: "prescription-pdf",
  initialState: {},
  reducers: {},
});

export const {} = prescriptionPdfSlice.actions;
export const prescriptionPdfReducer = prescriptionPdfSlice.reducer;
