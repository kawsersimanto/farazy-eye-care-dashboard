import { createSlice } from "@reduxjs/toolkit";

export const medicineCategorySlice = createSlice({
  name: "medicine-category",
  initialState: {},
  reducers: {},
});

export const {} = medicineCategorySlice.actions;
export const medicineCategoryReducer = medicineCategorySlice.reducer;
