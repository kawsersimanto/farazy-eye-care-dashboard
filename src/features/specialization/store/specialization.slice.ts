import { createSlice } from "@reduxjs/toolkit";

export const specializationSlice = createSlice({
  name: "specialization",
  initialState: {},
  reducers: {},
});

export const {} = specializationSlice.actions;
export const specializationReducer = specializationSlice.reducer;
